server:
  forward-headers-strategy: framework

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  flyway:
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    enabled: false
